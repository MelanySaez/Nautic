# =============================================================================
# PSCV-Lite-Nautic-Module — dependencias activas para desarrollo
# =============================================================================
# Las líneas comentadas con (#) pertenecen al requirements original del proyecto
# pero no son necesarias para esta fase de desarrollo (módulo de visión artificial
# + backend Django/DRF). Se conservan como referencia.
# =============================================================================

# --- Django core ---
Django==5.1.7
django-cors-headers==4.7.0
asgiref==3.8.1
sqlparse==0.5.3
python-dotenv==1.0.1

# --- Django REST Framework ---
djangorestframework==3.16.1

# --- Documentación automática de API ---
drf-spectacular==0.29.0

# --- Autenticación JWT ---
PyJWT==2.10.1
# djangorestframework_simplejwt==5.5.1   # en requirements original, no configurado en settings.py

# --- Base de datos PostgreSQL ---
psycopg2-binary==2.9.10
# psycopg2==2.9.10                        # duplicado; psycopg2-binary es suficiente para desarrollo

# --- Módulo de visión artificial (arrastra pillow, numpy, ultralytics) ---
nautic_core @ git+ssh://git@github.com/AaronDLF/nautic_core.git@v2.0.0

# --- Cola de tareas asíncronas (broker RabbitMQ vía AMQP) ---
celery==5.4.0
kombu==5.4.2

# --- Redis pub/sub (notificación SSE de worker → Django → frontend) ---
redis==5.2.1

# --- Monitoreo de workers Celery (UI web en :5555) ---
flower==2.0.1

# --- Requerido por api/views.py ---
qrcode==8.0

# --- HTTP / utilidades ---
requests==2.32.3
urllib3==2.2.3
certifi==2024.8.30
charset-normalizer==3.4.0
idna==3.10

# --- Utilidades generales de Python ---
typing_extensions==4.12.2
pytz==2024.2
tzdata==2024.2
python-dateutil==2.9.0.post0
six==1.16.0
setuptools==72.1.0

# =============================================================================
# DEPENDENCIAS COMENTADAS — no requeridas en esta fase
# =============================================================================

# --- Flask y su ecosistema (no se usa en este proyecto Django) ---
# Flask==3.0.0
# Flask-Bootstrap==3.3.7.1
# Flask-Cors==5.0.0
# Flask-Login==0.6.3
# Flask-MySQLdb==2.0.0
# Flask-Session==0.8.0
# Flask-SQLAlchemy==3.1.1
# Flask-WTF==1.2.1
# flask_weasyprint==1.1.0
# Werkzeug==3.0.1
# WTForms==3.1.2
# itsdangerous==2.1.2
# Jinja2==3.1.3
# MarkupSafe==2.1.3
# click==8.1.6
# blinker==1.8.2
# dominate==2.9.1

# --- Conectores de otras bases de datos (solo usamos PostgreSQL) ---
# SQLAlchemy==2.0.36
# greenlet==3.1.1
# PyMySQL==1.1.1
# pyodbc==5.2.0
# #mysqlclient==2.2.1

# --- Generación de PDF / reportes (no activa en esta fase) ---
# reportlab==4.2.2
# weasyprint==62.3
# svglib==1.5.1
# pypdf==5.1.0
# pydyf==0.11.0
# pyphen==0.16.0
# fonttools==4.54.1
# cssselect2==0.7.0
# tinycss2==1.4.0
# html5lib==1.1
# lxml==5.3.0
# webencodings==0.5.1
# zopfli==0.2.3.post1
# Brotli==1.1.0
# uritools==4.0.3
# visitor==0.1.3

# --- Texto árabe / bidi (no activo en esta fase) ---
# arabic-reshaper==3.0.0
# python-bidi==0.6.3

# --- Criptografía (no activa en esta fase) ---
# cryptography==43.0.3
# cffi==1.17.1
# pycparser==2.22
# asn1crypto==1.5.1
# oscrypto==1.3.0

# --- Análisis de datos / ciencia (no requerido directamente; nautic_core trae numpy) ---
# pandas==2.2.3
# scipy==1.14.1

# --- Otros no requeridos en esta fase ---
# openpyxl==3.1.5
# et_xmlfile==2.0.0
# qrcode==8.0
# redis==5.2.1
# msgspec==0.19.0
# PyYAML==6.0.2
# cachelib==0.13.0
# colorama==0.4.6
# distlib==0.3.8
# filelock==3.15.4
# platformdirs==4.2.2
# virtualenv==20.26.3
# tzlocal==5.2
# utils==1.0.2
