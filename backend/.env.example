# Application configuration
APP_NAME=Nautic Core API
APP_VERSION=1.0.0
APP_DESCRIPTION=Authentication and image processing API

# Security
SECRET_KEY=your-very-secure-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Vision (optional - auto-detected from nautic_core package if not set)
# MODEL_PATH=path/to/custom/model.pt

# Server
HOST=0.0.0.0
PORT=8000
RELOAD=True
